export interface ComponentProps {
    information: {
        title: string;
        description: string;
        maxWidth: string;
    }
}

export interface CatalogCategoriesInformation {
    title: string;
    description: string;
    maxWidth: string;
}